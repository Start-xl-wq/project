## 一、 录像场景与内存消耗

目前系统主要包含以下两个录像场景。考虑到最大内存占用量为 **14.5M**，测试时应**优先使用“高清录像”场景**进行极限覆盖。

![[Pasted image 20260509171620.png]]
---

## 二、 编译命令

请根据测试场景选择对应的编译命令：
### 高清录像场景

scons --target=board/jx009/rv-nds-a45/evb/a45_scenario_jx_evb.conf -j32 video_mode=12m

### 1080p录像场景

scons --target=board/jx009/rv-nds-a45/evb/a45_scenario_jx_evb.conf -j32

---

## 三、 测试目标与配置

- **配置文件**：`cam_config.h`
- **核心目标**：降低 `DUMP_SKIP_NUM` 的值，并确认在**摄像头运动状态下**，图像不能花屏、不能有错帧。
- **参数解析 (`DUMP_SKIP_NUM`)**：
    - 当前帧率：**30 fps**（即每帧间隔约 33ms）
    - 逻辑说明：`DUMP_SKIP_NUM` 的含义是跳过多少帧存一张图。
    - 举例计算：若当前设定为隔 19 帧存一张图，即存图间隔为：`(19 + 1) * 33ms = 660ms`。
![[Pasted image 20260509171726.png]]
---

## 四、 Buffer Layout 固件信息

- **BIN 文件路径**： `/proj/sw_bin/jx009/common_data/buffer_manager/layout_buffer_goer_ipm_v10.bin`
- **内存地址**：`0x8600c800`

---

## 五、 图像 Dump 分析指南

### 1. 查看 YUV 图像

- **使用工具**：`7yuv`
- **查看步骤**：
    1. 选择格式（Format）：**nv12**
    2. 填写尺寸（Size）：
        - 针对 `me dump`：填写 **1296 x 2304**
        - 针对 `be dump`：填写 **2528 x 3360**
    3. 参数设置无误后，图像即可正常显示。
    ![[Pasted image 20260509171847.png]]
![[Pasted image 20260509171817.png]]
### 2. 查看 RAW 图像

- **使用工具**：`GXRImageViewer`
- **尺寸与步长配置**：
    - **4K 场景**：Size **3024 x 4032**，Stride（步长）：**3792**
    - **3M 场景**：Size **2268 x 4032**，Stride（步长）：**2848**
![[Pasted image 20260509171933.png]]